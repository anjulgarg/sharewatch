#!/usr/bin/env node

/*!
 * sharewatch
 * Copyright(c) 2019 Anjul Garg <anjulgarg@live.com>
 * MIT Licensed
 */

'use strict'

/**
 * Module dependencies
 * @private
 */
// const program = require('commander')
// const packageFile = require('../package.json')

// program
//   .version(packageFile['version'])
//   .option('-p, --platform <p>', 'Valid choices are `bse` and `nse`', /^(bse|nse)$/i)
//   .option('-o, --output', 'Output file path')

// program
//   .command('equity-list')

// program
//   .command('indices')
//   // .option('-p, --platform <p>', 'Valid choices are `bse` and `nse`', /^(bse|nse)$/i)

// program
//   .command('nse-quote') 
//   .option('-isin, --isin', 'ISIN of the stock')

// program
//   .command('bse-quote') 
//   .option('-sc, --scrip-code', 'Scrip Code of the Stock')

// program
//   .parse(process.argv);
